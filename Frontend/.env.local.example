# Flask backend base URL for FactLayer API requests.
# The frontend calls this server directly from the browser (ensure CORS is enabled on Flask).
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Set to "true" only for local UI development without a running backend.
# Live API data always takes precedence when the backend responds successfully.
# NEXT_PUBLIC_USE_MOCK_DATA=false
